const BFS = require('./bfs.js');

class View {
  constructor($el) {
    this.$el = $el;
    
  }

  makeGrid() {
    let html = "";
    for (let i = 0; i < array.length; i++) {
      array[i]
    }
  }

  render() {

  }
}

module.exports = View;
